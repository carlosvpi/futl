module.exports = {
    filter: require('./filter'),
    fromInterval: require('./fromInterval'),
    map: require('./map'),
    merge: require('./merge'),
    reduce: require('./reduce'),
    stream: require('./stream'),
    times: require('./times')
}
